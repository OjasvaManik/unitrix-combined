import { Architects_Daughter, Ballet } from "next/font/google";

export const architectsDaughter = Architects_Daughter( {
  subsets: [ 'latin' ],
  weight: [ '400' ],
  fallback: [ 'mono' ]
} );

export const ballet = Ballet( {
  subsets: [ 'latin' ],
  weight: [ '400' ],
  fallback: [ 'mono' ]
} );